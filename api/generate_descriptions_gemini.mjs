// CLI script moved to scripts/generate_descriptions_gemini.mjs
export default function handler(req, res) {
    res.status(200).json({ message: "CLI script moved to scripts/generate_descriptions_gemini.mjs" });
}
